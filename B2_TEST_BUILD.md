# NYX Backblaze B2 test build

This test build uses the configured Backblaze B2 application credentials directly in the Android upload worker and uploads to the `Nyxoria` bucket.

**Important:** these credentials are intentionally embedded only because this is a private/test build. Rotate the application key before distributing a production APK.
